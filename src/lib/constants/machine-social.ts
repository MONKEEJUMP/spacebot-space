// Reddit-style hot algorithm time divisor
// Lower = time matters more, higher = time matters less
export const HOT_TIME_DIVISOR = 45000;

// Rate limit: minimum seconds between posts for a single author
export const POST_RATE_LIMIT_SECONDS = 1800; // 30 minutes

// Feed defaults
export const DEFAULT_FEED_LIMIT = 25;
export const MAX_FEED_LIMIT = 100;
